<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@android:color/white">

  <include layout="@layout/merge_appbar" />

  <com.stfalcon.chatkit.messages.MessagesList
    android:id="@+id/messagesList"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_above="@+id/divider"
    android:layout_marginTop="?attr/actionBarSize"
    app:incomingBubbleDrawable="@drawable/shape_incoming_message"
    app:incomingDefaultBubbleColor="@android:color/white"
    app:outcomingBubbleDrawable="@drawable/shape_outcoming_message"
    app:outcomingDefaultBubbleColor="@color/primary" />

  <View
    android:id="@+id/divider"
    android:layout_width="match_parent"
    android:layout_height="1dp"
    android:layout_above="@+id/input"
    android:layout_gravity="bottom"
    android:layout_marginLeft="16dp"
    android:layout_marginRight="16dp"
    android:background="@color/divider" />

  <com.stfalcon.chatkit.messages.MessageInput
    android:id="@+id/input"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentBottom="true"
    android:background="@android:color/white"
    app:attachmentButtonBackground="?attr/selectableItemBackgroundBorderless"
    app:attachmentButtonDefaultIconColor="@color/primary"
    app:attachmentButtonDefaultIconPressedColor="@color/primary_dark"
    app:attachmentButtonHeight="27dp"
    app:attachmentButtonWidth="27dp"
    app:inputButtonDefaultBgColor="@color/primary"
    app:inputButtonDefaultBgPressedColor="@color/primary_dark"
    app:inputButtonDefaultIconColor="@android:color/white"
    app:inputButtonDefaultIconDisabledColor="@color/background_lightish_3"
    app:inputHint="@string/chat_hint_enter_a_message"
    app:inputMaxLines="3"
    app:showAttachmentButton="true" />

</RelativeLayout>